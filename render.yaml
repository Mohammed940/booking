services:
  - type: web
    name: medical-booking-bot
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: SPREADSHEET_ID
        sync: false
      - key: GOOGLE_CREDENTIALS
        sync: false
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000